# Журнал разработки SuperPart.ru

## 14.04.2026 -- Сидер: по одному пользователю на роль (образцы)

- `database/seeders/RoleSampleUsersSeeder.php`: четыре учётные записи (`roles-sample-developer@`, `roles-sample-director@`, `roles-sample-partner@`, `roles-sample-manager@` на домене `superpart.ru`), пароль `password`; менеджер привязан к партнёру из сида; города как у остальных партнёров; источники по шаблону SourceSeeder, у менеджера pivot `allowed_sources` на источники партнёра.
- `database/seeders/DatabaseSeeder.php`: вызов `RoleSampleUsersSeeder` после `SourceSeeder` (чтобы не дублировать создание источников для этих пользователей в общем проходе).

## 14.04.2026 -- Адаптив: бургер-меню и статистика на главной

- `resources/views/partials/navbar.blade.php`: группы навигации собраны в массив `$navGroups` (в т.ч. условный блок «Управление» через `array_splice`); десктоп — цикл `x-dropdown-menu` в `hidden lg:flex`; на ширине &lt; `lg` — компактная строка (баланс, «Создать», бургер), выпадающее меню профиля только на `lg+`; выезжающая панель слева с теми же разделами, настройками и выходом (`fixed`, `z-[60]`, оверлей).
- `resources/js/app.js`: `initMobileNav()` — открытие/закрытие по кнопке, клику по фону, Escape; блокировка прокрутки `body` при открытии; закрытие по ссылкам внутри шторки; иконка бургер/крестик; при переходе ширины ≥ 1024px шторка закрывается (`matchMedia`); сборка Vite обновлена.
- `resources/views/dashboard/index.blade.php`: блок быстрой статистики — `grid-cols-1` и `divide-y` на малых экранах, `lg:grid-cols-5` и `divide-x` на больших; у ячеек `min-w-0`, `tabular-nums`, `break-words`.

## 14.04.2026 -- Страница «Настройки»: API, карты, телефоны, заглушка комиссий

- Эталон вёрстки блока API: `Import/settings-api-reference.png`.
- Миграции: `2026_04_14_210000_add_api_credentials_to_users_table.php` (`api_login`, зашифрованный `api_password`, `partner_code`); `2026_04_14_210001_create_partner_bank_cards_table.php`; `2026_04_14_210002_create_partner_phones_table.php` (FK на `sources` и `reference_sources`).
- Модели `PartnerBankCard`, `PartnerPhone`; у `User` — связи `partnerBankCards()`, `partnerPhones()`, в `$fillable` и `casts` для `api_password` (encrypted).
- `app/Http/Requests/Settings/*`: сохранение API, карты (валидация 16 цифр, банк до 20 букв, ФИО по regex), пакетное обновление телефонов, добавление телефона (только роль разработчик).
- `app/Http/Controllers/SettingsController.php`: `updateApiCredentials`, CRUD карт, `updatePartnerPhones`, `storePartnerPhone`; список источников для телефонов по той же логике, что `OrderController` (CRM / локальные `sources`, ограничения для менеджера).
- `routes/web.php`: маршруты `settings.api-credentials`, `settings.bank-cards.*`, `settings.phones.*`.
- `resources/views/settings/index.blade.php`: один столбец «Профиль» + «Смена пароля»; секции «Данные API» (инпут + «Придумать»), «Цена заявки» (статичные значения, комментарий про будущие данные), «Банковские карты», «Телефоны» (сохранение источников; добавление номера — только для разработчика); переключатель темы — активное состояние через `bg-secondary` + `ring-primary` для читаемого текста.

## 14.04.2026 -- Справочники: сотрудники (ФИО, почта, пароль), города, виды работ

- Миграция `2026_04_14_120000_add_profile_and_last_login_to_users_table.php`: `users.last_login_at`, `last_name`, `first_name`, `middle_name` (nullable).
- `app/Models/User.php`: fillable, cast `last_login_at`, метод `displayFullName()`.
- `app/Support/PersonName.php`: правила частей ФИО, сборка `name`, разбор legacy-строки `name` (одно слово — заглушка «Уточните» в имени для прохождения валидации).
- `app/Support/PortalUserProvisioning.php`: длина генерируемого пароля по умолчанию 10 символов.
- `app/Http/Controllers/AuthController.php`: обновление `last_login_at` при успешном входе.
- `app/Http/Controllers/Management/ManagementUserController.php`: полный email до 50 символов; создание/редактирование с ФИО и паролем (10 символов или автогенерация); опциональное поле `new_password` при редактировании; метод `destroy` с проверками связей; удалены правила `email_prefix`.
- `routes/web.php`: маршрут `DELETE management/users/{user}` (`management.users.destroy`).
- `resources/views/partials/navbar.blade.php`: «Список сотрудников» в «Справочники»; пункт «Пользователи» убран из «Управление» (меню «Управление» только при «Источники CRM»).
- `resources/views/management/users/index.blade.php`, `create.blade.php`, `edit.blade.php`: подписи и крошки «Справочники», таблица ID / почта / ФИО / статус (роль) / последний визит, кнопки «Создать сотрудника», «Сохранить» / «Закрыть», генератор пароля, удаление сотрудника.
- `app/Http/Controllers/CityController.php`, `resources/views/cities/index.blade.php`: столбец «Доступ партнера»; фильтр `available` со значениями `1` / `0` / `all` (по умолчанию «Да»), исправлен сброс «Все» → «Да» при отсутствии параметра в запросе.
- `database/seeders/WorkTypeSeeder.php`: обновлены тексты столбца «Описание» по ТЗ.
- `deploy/ftp/deploy_260414_1.sh`: в тот же скрипт выгрузки за 14.04.2026 добавлены файлы блока «Справочники / сотрудники» и команда `php83 artisan db:seed --class=WorkTypeSeeder --force` в блоке SSH.

## 14.04.2026 -- Отчёты: строка «Итого» в светлой теме

- `resources/views/reports/cities.blade.php`, `work-types.blade.php`, `statistics.blade.php`: вместо `bg-secondary` (тёмный токен и в светлой теме) — `bg-card`; разделитель сверху — `border-t-2 border-foreground/20 dark:border-border`.

## 14.04.2026 -- Начисления: фильтр ID, переход в заявку, страница вывода д/с

- `resources/views/components/ui/filter-input.blade.php`: проп `narrow` (узкая колонка), `autocomplete="off"` у поля.
- `resources/views/transactions/index.blade.php`: компактная ячейка фильтра по ID (`narrow`, `inputmode="numeric"`); клик по строке с `order_id` ведёт на `orders.show`; обработчик клика по `tr[data-href]` (как на списке выводов).
- `resources/views/withdrawals/index.blade.php`: компактное поле поиска по ID заявки на вывод.
- `resources/views/withdrawals/form.blade.php` (режим просмотра заявки): блоки реквизитов/комментария с белым фоном (`bg-white`, тёмный текст для читаемости); подсказки про ФИО/банк только при создании; три неактивные кнопки-«капсулы»: «Добавить комментарий», «Претензия», «Негативный отзыв».

## 14.04.2026 -- Раздел «Заявки»: навбар, список, карточка заявки

- `resources/views/partials/navbar.blade.php`: из выпадающего меню «Заявки» убран пункт «Не оформленные» (маршрут `orders.unprocessed` и страница сохранены для прямых ссылок).
- `resources/views/orders/index.blade.php`: убрана кнопка «Создать» у вкладок статусов (дублирует кнопку в навбаре); экспорт остаётся.
- `resources/views/orders/partner-order-detail.blade.php`: блок «Действия по заявке» с четырьмя неактивными кнопками (вёрстка): «Добавить комментарий», «Отменить заявку», «Претензия», «Негативный отзыв».

## 14.04.2026 -- Создание заявки: чекбокс «Без звонка»

- Миграция `2026_04_14_120000_add_without_call_to_orders_table.php`: колонка `orders.without_call` (boolean, по умолчанию false).
- `app/Models/Order.php`: поле в `$fillable` и `casts`.
- `app/Http/Controllers/OrderController.php` (`store`): валидация и сохранение `without_call` через `$request->boolean('without_call')`.
- `app/Services/LevelionApiService.php` (`pushPartnerOrder`): в JSON для CRM добавлено поле `without_call`.
- `resources/views/orders/create.blade.php`: чекбокс под карточкой формы, над блоком кнопок «Отмена» / «Создать заявку».
- `resources/views/orders/partner-order-detail.blade.php`: строка «Без звонка: Да/Нет».
- `deploy/ftp/deploy_260414_1.sh`: в список выгрузки добавлены миграция и файлы фичи; в блоке SSH — `php83 artisan migrate --force`.

## 14.04.2026 -- Фильтры таблиц: без иконки поиска, debounce отправки формы, «Вид» на главной

- `resources/views/components/ui/filter-input.blade.php`: убрана кнопка с иконкой лупы; поле без дополнительного отступа справа.
- `resources/js/app.js`: вместо `initFilterSearchButtons()` — `bindFilterFormSubmits` / `window.superpartBindFilterForm` (select — сразу `requestSubmit`, текстовые поля — debounce 350 ms + Enter); из «липкой» шапки таблицы удалены обработчики клика по лупе.
- Страницы с серверной фильтрацией: `dashboard/index`, `orders/index`, `orders/unprocessed`, `reports/orders`, `withdrawals/index`, `transactions/index` — подключён `superpartBindFilterForm`.
- `resources/views/cities/index.blade.php`: для полей имени и ID города — debounce + Enter к `applyFilters` (без лупы).
- `app/Http/Controllers/DashboardController.php`: в подписи вида заявки для фильтра добавлен ключ `first_time` → «Впервые» (раньше в выпадалке показывалось `first_time`).

## 14.04.2026 -- Навбар: тема только в настройках, баланс без дублирования

- `resources/views/partials/navbar.blade.php`: убрана кнопка переключения темы (`#theme-toggle`); смена темы остаётся на странице настроек.
- `resources/views/layouts/app.blade.php`: убран дублирующий `<x-balance-badge>` из блока над контентом (ряд с хлебными крошками).
- `resources/views/transactions/index.blade.php`: убрана строка «Баланс» над фильтром; колонка «Остаток» в таблице без изменений.
- `app/Http/Controllers/TransactionController.php`: из `view()` убрана неиспользуемая переменная `balance`.

## 13.04.2026 -- Интеграция источников CRM (SUPERPART_SOURCES): справочник, PATCH, source_id в заявке

- Миграции: `reference_sources` (кэш GET `reference/sources`: `levelion_source_id`, название, город CRM, `superpart_partner_id`, `synced_at`); `orders.reference_source_id` (FK); pivot `user_allowed_reference_sources` (уникальный индекс `uarfs_user_ref_unique`).
- Модель `ReferenceSource`, связи у `User::allowedReferenceSources()`, у `Order::referenceSource()`; отображение названия: `Order::sourceDisplayName()` / `App\Support\OrderSourceFilter` (ключи фильтров `r{id}` / `s{id}` для CRM и локальных заявок).
- `LevelionApiService`: `syncReferenceSourcesFromLevelion()`, `patchReferenceSourcePartner()` (PATCH `reference/sources/{id}` с подписью от JSON-тела); `pushPartnerOrder` передаёт в CRM `source_id` = `levelion_source_id` при наличии `reference_source_id` у заявки.
- При настроенном LEVELION_*: форма создания заявки — выбор из CRM-каталога с фильтром «общий или закреплён за партнёром»; менеджеры — пересечение с `user_allowed_reference_sources`. Без CRM — прежняя логика локальных `sources`.
- `ManagementReferenceSourceController`: список каналов и назначение партнёра (вызов PATCH + повторная синхронизация); маршруты `management/reference-sources`, Gate `access-management-reference-sources` (гендиректор и разработчик); пункт «Источники CRM» в навбаре «Управление».
- `ManagementUserController`: при CRM источники для менеджеров — `reference_sources`, синхронизация `allowedReferenceSources`; иначе — `allowedSources`. Индекс пользователей и форма редактирования учитывают режим.
- `DashboardController`, `ReportController`, `OrderController`: фильтры по источнику через `OrderSourceFilter`; отчёт «Статистика» — группировка по CRM- и локальным источникам; экспорт заявок — `sourceDisplayName()`.
- `SourceController` / `sources/index`: пояснение при включённой интеграции с CRM.

## 13.04.2026 -- Города: назначение партнёру, фильтры по назначенным, /cities без менеджера

- Модель `User`: метод `hasRestrictedCityAccess()` (партнёр или менеджер).
- `App\Support\PortalCityOptions`: ограничение выбора города в форме заявки/отзыва; карта городов для фильтров заявок; названия городов для фильтра отзывов.
- `ManagementUserController`: у партнёра в форме списка городов — только назначенные ему; валидация городов менеджера — `Rule::in` по городам партнёра; при роли «партнёр» обязательны `city_ids`, синхрон `user_allowed_cities`; разработчик/гендиректор по-прежнему без городов в pivot.
- `OrderController`, `DashboardController`, `ReportController`, `ReviewController`: партнёр и менеджер видят в фильтрах и формах только назначенные города; проверка города при создании заявки и `cityTime`; убран fallback «все города» для не-менеджера.
- Gate `access-cities-directory`, маршрут `GET /cities` с `can:access-cities-directory`; менеджер получает 403, пункт «Актуальные города» в навбаре скрыт.
- `CityController@index`: `authorize('access-cities-directory')`.
- `resources/views/cities/index.blade.php`: убран столбец «Загруженность».
- `resources/views/management/users/create.blade.php`, `edit.blade.php`: блок «Города партнёра» для роли partner; подсказка только для разработчика/гендиректора; отключение полей в скрытых блоках при submit.
- Сидеры `UserSeeder`, `IntegrationTestPartnerSeeder`: назначение городов партнёрам для локальных данных.

## 13.04.2026 -- Источники: карточка = форма, строка таблицы, отзыв, мягкое удаление

- Миграция `2026_04_13_210000_add_soft_deletes_to_sources_table.php`: поле `sources.deleted_at` (soft delete).
- Модель `Source`: трейт `SoftDeletes`; статические правила валидации `existsRuleForOwnerId()`, `existsRuleActive()` (учёт `deleted_at`).
- `Order::source()`: `withTrashed()`, чтобы в карточке заявки сохранялось название источника после «удаления».
- `SourceController`: `show`, `update`, `destroy`; отдельной страницы `edit` нет; после `store` редирект на `sources.show`; выборка по `user_id` = `effectiveOwnerId()`; правки — только при `manage-sources`.
- Маршруты: `GET/PUT/DELETE /sources/{id}`; `GET /sources/{id}/edit` — редирект 301 на карточку; удалён `resources/views/sources/edit.blade.php`.
- `resources/views/sources/show.blade.php`: для партнёра одна карточка-форма; «Сохранить», «К списку» и «Удалить» в одной строке (кнопка сохранения с `form="source-update-form"`); группа «Отзыв» с `x-ui.form-group` и `x-ui.checkbox` с пропом `:checked`.
- `resources/views/components/ui/checkbox.blade.php`: проп `checked` и `@checked` на `<input>` (директива `@checked` на `<x-ui.checkbox>` раньше не доходила до input).
- `resources/views/sources/create.blade.php`: то же скрытое поле для `has_review`.
- `resources/views/sources/index.blade.php`: клик по строке (и Enter/Пробел) открывает карточку; колонка действий убрана.
- `OrderController::store`: валидация `source_id` через `Source::existsRuleForOwnerId`.
- `ManagementUserController`: правила для `source_ids.*` — `existsRuleForOwnerId` / `existsRuleActive`.

## 13.04.2026 -- Сброс пароля при редактировании пользователя

- Маршрут `POST management/users/{user}/reset-password` (`management.users.reset-password`), метод `ManagementUserController::resetPassword`: те же права, что и у редактирования; новый пароль через `PortalUserProvisioning::generatePassword()`, сохранение с учётом cast `password` => `hashed`; редирект на `users.edit` с `success` и `generated_password`.
- `App\Support\UserSessionRevoker::revokeAll()`: при `session.driver` = `database` удаление строк `sessions` с `user_id` пользователя; смена `remember_token` — сброс «запомнить меня».
- `resources/views/management/users/edit.blade.php`: убраны поля пароля; форма редактирования с `id="management-user-edit-form"`, кнопка «Сохранить» с `form="…"`; в одной строке с «Сохранить» и «Отмена» — отдельная форма «Сбросить пароль» (`confirm` про сессии); сверху — алерт и карточка с паролем при `generated_password`.
- `ManagementUserController::updateManagerForPartner` / `updateElevated`: удалены валидация и сохранение пароля из формы обновления.

## 13.04.2026 -- Форма пользователя: сетка имя/логин/роль, города/источники, комментарий

- Миграция `2026_04_13_200000_add_comment_to_users_table.php`: поле `users.comment` (text, nullable).
- Модель `User`: `comment` в `$fillable`.
- `ManagementUserController`: валидация и сохранение `comment` при создании и обновлении (все ветки).
- `resources/views/management/users/create.blade.php`, `edit.blade.php`: контейнер `max-w-4xl`; первая строка — три колонки (имя, логин или почта, роль); вторая — два столбца «Города» и «Источники» (у повышенной роли для менеджера выше полоса «Партнёр-владелец»); затем «Комментарий»; на редактировании пароль — две колонки под комментарием.
- `resources/views/components/multiselect-checkboxes.blade.php`: корень принимает `$attributes` (например `class="!mb-0"` в сетке).

## 13.04.2026 -- Управление пользователями: без дубля «Сотрудники», @sp.ru, генерация пароля, мультиселект

- `GET /employees` и `/employees/create` — редирект на `management/users` и `management/users/create`; удалены `EmployeeController`, шаблоны `resources/views/employees/*`, gate `access-employees-directory`; пункт «Сотрудники» убран из навбара (`resources/views/partials/navbar.blade.php`).
- `App\Support\PortalUserProvisioning`: суффикс почты `@sp.ru`, генерация пароля (как в CRM), нормализация локальной части, `isSpDomainEmail()` для формы редактирования.
- `ManagementUserController`: создание без пароля из формы — пароль генерируется, в сессии `generated_password`; поле `email_prefix` + уникальность полного email; роль при создании — только из `roleOptionsForAuth()` (партнёр — менеджер); правки для менеджера/повышенных ролей; при редактировании пользователя с почтой не на `@sp.ru` — по-прежнему полное поле email до перевода на домен портала.
- `resources/views/management/users/create.blade.php`, `edit.blade.php`: `autocomplete="off"`, логин + суффикс `@sp.ru`, кастомный мультиселект `x-multiselect-checkboxes` для городов и источников; партнёр — скрытые роль и родитель.
- `resources/views/management/users/index.blade.php`: одноразовый показ сгенерированного пароля и кнопка «Копировать».
- `resources/views/components/multiselect-checkboxes.blade.php`: проп `error`, подсветка границы при ошибке валидации поля.

## 13.04.2026 -- Роли, управление пользователями, статусы CRM, навбар

- Миграция `2026_04_13_140000_user_roles_and_acl.php`: поля `users.role`, `users.parent_user_id`; таблицы `user_allowed_cities`, `user_allowed_sources`; сидер `UserSeeder` — `admin@superpart.ru` как разработчик, остальные как партнёры.
- Модель `User`: константы ролей, связи `parent`, `managedUsers`, `allowedCities`, `allowedSources`, `visibleOrderUserIds()`, `effectiveOwnerId()`, проверки `isDeveloper` / `isGeneralDirector` / `isPartner` / `isManager` / `hasElevatedAccess`.
- `AppServiceProvider`: `Gate::before` для dev/гендиректора; `use-withdrawals`, `access-management-users`, `access-employees-directory`, `manage-sources`.
- `Order::scopeForPortalUser`, `Review::scopeForPortalUser`; контроллеры заявок, дашборда, транзакций, отчётов, отзывов — видимость данных по «организации» партнёра (свои заявки + менеджеров); `OrderController`: создание заявки и ACL городов/источников для менеджера; `show` — `LevelionApiService::syncOrderStatusFromCrmIfConfigured`.
- `WithdrawalController`: `Gate::authorize('use-withdrawals')` (менеджер без выплат); `EmployeeController` — справочник сотрудников; `SourceController` — источники владельца (`effectiveOwnerId`), создание только при `manage-sources`.
- `Management\ManagementUserController`, маршруты `management/users*`, представления `resources/views/management/users/*`: партнёр создаёт только менеджеров с городами/источниками; разработчик/ГД — любые роли.
- `App\Support\CrmOrderStatusMapper`, `LevelionApiService::fetchPartnerOrderFromCrm`, `syncOrderStatusFromCrmIfConfigured`.
- `LevelionWebhookController::partnerOrderCreated` — заглушка без `Order::create`; миграция `2026_04_13_150000_normalize_order_status_enum.php` — нормализация статусов и сужение MySQL enum; подписи статусов в `OrderController`, `ReportController`, `DashboardController`, `status-badge`, `OrdersExport`.
- Навбар: `route()` для пунктов; «Управление → Пользователи»; выплаты в меню только при `use-withdrawals`; сотрудники в справочниках при `access-employees-directory`; кнопка «Создать» → `orders.create`.
- `tests/Unit/CrmOrderStatusMapperTest.php`; `docs/DEPLOY.md` — блок про вебхуки и источник заявок.

## 13.04.2026 -- Форма заявки: город в строке «Время в городе», выравнивание селекта

- `resources/views/orders/create.blade.php`: убран дублирующий вывод названия города под селектом (`#orderCityLabel` и лишние обёртки `flex`); селект города в одной колонке как улица/дом — те же отступы к контролу, что у `x-ui.input`; название города показывается в одной строке с временем: `ЧЧ:ММ (Город)` в `#orderLocalTime`; блок «Время в городе клиента» — `!mb-0` вместо лишнего `mb-4` у третьей колонки.

## 13.04.2026 -- Телефон без тени, адрес четыре равные колонки

- `resources/views/components/input-phone-ru.blade.php`: убраны `shadow-xs` и переход по box-shadow; обёртка `+7` / поле без тени.
- `resources/views/orders/create.blade.php`: строка город/улица/дом/кв — `lg:grid-cols-4`, прямые потомки с `*:min-w-0`, селект города `w-full min-w-0`, сняты узкие `max-w` у дома и квартиры; брейкпоинт четырёх колонок с `xl` на `lg`.
- `npm run build`, `deploy/ftp/deploy_260413_5.sh` — Blade + `public/build`.

## 13.04.2026 -- Форма «Новая заявка»: сетка через Tailwind + сборка public/build

- `resources/views/orders/create.blade.php`: строки полей через утилиты Tailwind (`grid`, `grid-cols-*`, `gap-*`, `xl:grid-cols-4` для города/адреса), блок «Время в городе клиента» — `flex`/`min-h`/`text-muted-foreground`; у поля возраста стрелки number скрыты через arbitrary-классы (`[appearance:textfield]`, webkit spinners); без опоры на кастомные классы из `app.css`, чтобы стили гарантированно попадали в Vite-бандл.
- `resources/css/app.css`: удалён неиспользуемый блок стилей формы заявки (дублировал Tailwind).
- `npm run build`: новые `public/build/manifest.json`, `public/build/assets/app-oz3q14pL.css`, `app-C2JHiaRZ.js`.
- `deploy/ftp/deploy_260413_4.sh`: выгрузка Blade + `public/build/*` для продакшена.

## 13.04.2026 -- Форма «Новая заявка» как в CRM (persons/create)

- Миграция `database/migrations/2026_04_13_120000_order_form_crm_alignment.php`: поля `cities.timezone`, `orders.client_age`; расширение `orders.status` (MySQL enum) значениями `callback`, `pending`, `cancelled_cc`, `cancelled_city`.
- Классы `App\Support\PersonClientValidation` (телефон 10 цифр, фрагменты адреса, max длина комментария к адресу) и `App\Support\OrderEquipment` (виды техники и core/non_core как в Lead Control).
- `OrderController`: форма и `store` по полям CRM — `order_type` (new/repeat/warranty), маппинг на `type` first_time/repeat/warranty; обязательные `equipment_type`, `status`, `source_id`, `order_date` + `meeting_time`; адрес по правилам CRM; `work_type_id`/`employee_id`/`settlement` не задаются; маршрут `GET /orders/city/{city}/time` (`cityTime`) для JSON времени города.
- `LevelionApiService`: при синхронизации городов подтягивается `timezone` из ответа API при наличии; `pushPartnerOrder` отправляет телефон через `normalizeClientPhoneForApi` (+7 и 10 цифр).
- `resources/views/orders/create.blade.php`: порядок полей и блок «Данные заказа» как в CRM; скрипт обновления даты/времени и «Время в городе клиента».
- `resources/views/components/input-phone-ru.blade.php`, `public/js/phone-mask.js`, подключение в `resources/views/layouts/app.blade.php`.
- Обновлены подписи и цвета статусов: `resources/views/components/status-badge.blade.php`, `app/Exports/OrdersExport.php`, `DashboardController`, `ReportController` (в т.ч. accepted/rejected для отчётов).
- `resources/views/orders/partner-order-detail.blade.php`: возраст клиента, формат телефона, подпись вида техники по `OrderEquipment`.

## 11.04.2026 -- FTP: deploy_260411_13 (LevelionApiService)

- Добавлен `deploy/ftp/deploy_260411_13.sh`: выгрузка `app/Services/LevelionApiService.php` (FTP `u3398705_cursor_sp`).

## 11.04.2026 -- LevelionApiService: подсказка при HTTP 500 (логи CRM)

- В `formatPartnerOrderErrorHint` для HTTP 500 добавлено напоминание смотреть `storage/logs/laravel.log` на сервере Lead Control.

## 11.04.2026 -- Levelion API: LEVELION_API_PATH_PREFIX, crmApiUrl(), подсказка при HTTP 404

- В `config/services.php` добавлен `levelion.api_path_prefix` (`LEVELION_API_PATH_PREFIX`, по умолчанию `api/v1`); в `LevelionApiService` URL к CRM собираются через `crmApiUrl()` для cities, work-types, partner-orders.
- В `formatPartnerOrderErrorHint` для HTTP 404 добавлено пояснение про выгрузку `routes/api.php` / `PartnerOrderController` на CRM и про корректный `LEVELION_BASE_URL`.
- Обновлены `.env.example` и `deploy/regru/.env.example`.

## 11.04.2026 -- CRM: flash после создания заявки с текстом sync_last_error

- В `app/Http/Controllers/OrderController.php` при `sync_status === error` в зелёный алерт добавляется `sync_last_error` или подсказка про `.env` и `laravel.log`, вместо общей фразы «проверьте настройки интеграции».

## 11.04.2026 -- CRM: sync_last_error, расшифровка ошибки в карточке заявки

- Миграция `database/migrations/2026_04_11_130000_add_sync_last_error_to_orders_table.php`: поле `orders.sync_last_error`.
- `LevelionApiService::pushPartnerOrder` возвращает `['crm_id' => …, 'error' => …]`; в `error` — HTTP-код, сообщение CRM из JSON или фрагмент тела ответа.
- `OrderController@store` и модель `Order` сохраняют `sync_last_error`; при отсутствии `levelion_city_id` при настроенном Levelion — свой текст причины.
- `resources/views/orders/partner-order-detail.blade.php`: блок «Причина» на всю ширину карточки при наличии `sync_last_error`.
- `deploy/ftp/deploy_260411_10.sh` — выгрузка этих файлов (FTP `u3398705_cursor_sp`).

## 11.04.2026 -- FTP: deploy_260411_9 (форма заявки + button + layout)

- Добавлен `deploy/ftp/deploy_260411_9.sh`: выгрузка `OrderController.php`, `button.blade.php`, `app.blade.php`, `orders/create.blade.php`; FTP `u3398705_cursor_sp`.

## 11.04.2026 -- Форма «Новая заявка»: submit, flash status, ошибки города CRM

- `resources/views/components/ui/button.blade.php`: тип кнопки берётся из `$attributes` (`type="submit"` не теряется), дублирование `type` в разметке убрано.
- `resources/views/layouts/app.blade.php`: вывод `session('status')` зелёным алертом (редиректы с `->with('status', …)`, в т.ч. после создания заявки).
- `app/Http/Controllers/OrderController.php`: при городе без `levelion_city_id` при настроенном Levelion — `withErrors(['city_id' => …])` вместо невидимого `with('status')`.
- `resources/views/orders/create.blade.php`: блок списка ошибок валидации, `id` формы, при отправке подпись кнопки «Сохранение…» и `disabled`.

## 11.04.2026 -- FTP: объединённый deploy_260411_8, пользователь u3398705_cursor_sp

- Добавлен `deploy/ftp/deploy_260411_8.sh`: одна выгрузка за 11.04.2026 — миграция Levelion, bootstrap, API, middleware, `LevelionApiService`, `OrderController`, провайдер, сидеры, `vendor/composer/autoload_*.php`, `partner-order-detail.blade.php`, `layouts/app.blade.php`; перед FTP — `composer dump-autoload -o`; FTP-пользователь `u3398705_cursor_sp` (домашний каталог в панели — корень Laravel SuperPart).

## 11.04.2026 -- Деплой: deploy_260411_7, маркер superpart-layout в app layout

- В `resources/views/layouts/app.blade.php` перед `</body>` добавлен HTML-комментарий `superpart-layout:20260411` — по нему на любой странице видно, что выгрузка попала в нужный каталог Laravel.
- Добавлен `deploy/ftp/deploy_260411_7.sh`: FTP одновременно `OrderController.php`, `partner-order-detail.blade.php`, `app.blade.php`; в шапке — проверки grep и напоминание про OPcache/перезапуск PHP.

## 11.04.2026 -- Карточка заявки: шаблон partner-order-detail вместо show

- Карточка перенесена в `resources/views/orders/partner-order-detail.blade.php`; `OrderController@show` вызывает `view('orders.partner-order-detail')`, чтобы не упираться в устаревший `show.blade.php` на сервере. В шаблоне добавлен HTML-комментарий `superpart:order-detail:20260411` для проверки выгрузки; строка «Синхронизация» выводится всегда.
- Удалён `resources/views/orders/show.blade.php`.
- Добавлен `deploy/ftp/deploy_260411_6.sh` — выгрузка `OrderController.php` и `partner-order-detail.blade.php`.
- Обновлён `docs/prompts/08_СТРАНИЦА_КАРТОЧКА_ЗАКАЗА.md` (путь к шаблону).

## 11.04.2026 -- FTP: deploy_260411_5 (карточка заявки)

- Добавлен `deploy/ftp/deploy_260411_5.sh`: выгрузка `resources/views/orders/show.blade.php` на SuperPart (reg.ru).

## 11.04.2026 -- Карточка заявки: всегда показывать «ID в CRM»

- В `resources/views/orders/show.blade.php` строка «ID в CRM» выводится всегда; при отсутствии номера — «—»; подписи статуса синхронизации: ожидает отправки / отправлено в CRM / не удалось отправить в CRM.

## 11.04.2026 -- FTP: deploy_260411_3 (SuperPart полный + CRM отдельно)

- `superpart.ru/deploy/ftp/deploy_260411_3.sh`: перед выгрузкой выполняется `composer dump-autoload -o`; в FTP — сидеры, `LevelionWebhookController`, `vendor/composer/autoload_classmap.php` и `autoload_static.php`.
- `Levelion_dev/deploy/ftp/deploy_260411_3.sh`: FTP пользователя CRM `u3398705_cursor_lc`, файл `PartnerOrderController.php`.

## 11.04.2026 -- Сидер: require_once в AppServiceProvider (прод без актуального composer)

- В `app/Providers/AppServiceProvider.php` при консоли выполняется `require_once` для `IntegrationTestPartnerSeeder.php`, если файл есть — чтобы `artisan db:seed --class=IntegrationTestPartnerSeeder` находил класс без обновлённого classmap на сервере.
- В `deploy/ftp/deploy_260411_3.sh` в список FILES добавлен `AppServiceProvider.php`.

## 11.04.2026 -- CRM: PartnerOrderController, скрипт FTP deploy_260411_1

- В репозитории Levelion (`Levelion_dev`): в `app/Http/Controllers/Api/PartnerOrderController.php` проверка «order_core или equipment_type» использует `?? null`, чтобы не обращаться к отсутствующим ключам в `$validated` (PHP 8.2+).
- Добавлен `deploy/ftp/deploy_260411_1.sh` — выгрузка на reg.ru файлов сидера интеграции, `DatabaseSeeder`, `LevelionWebhookController`; в шапке команды для сервера и напоминание про отдельную выгрузку `PartnerOrderController` на lead-control.
- `deploy_260411_1.sh` переписан по образцу скриптов Levelion (`deploy/ftp/deploy_*.sh`): шапка с разделителем `=====`, дата из имени файла, массив `FILES`, пофайловый `curl --ftp-create-dirs` (без lftp/mirror), счётчики OK/ERROR/SKIP, блок SSH-команд; пользователь FTP SuperPart `u3398705_cursor_testir`.

## 11.04.2026 -- Деплой: composer dump-autoload после нового сидера

- В `docs/DEPLOY.md` (раздел «Команды на сервере»): после точечной FTP-выгрузки новых классов в `database/seeders/` без заливки `vendor/` на сервере нужно выполнить `composer dump-autoload`, иначе ошибка `Target class [Database\Seeders\…] does not exist`.
- В `deploy/ftp/deploy_260411_1.sh` в блоке команд для SSH добавлены `composer dump-autoload` и пояснение в шапке скрипта.
- На reg.ru в SSH часто нет `composer`: в `DEPLOY.md` и `deploy_260411_1.sh` описан обход — локально `composer dump-autoload`, FTP файлов `vendor/composer/autoload_classmap.php` и `autoload_static.php`; эти файлы добавлены в `FILES` скрипта выгрузки.

## 11.04.2026 -- Интеграция Levelion: сидер тестового партнёра, потолок начисления на бэкенде

- Добавлен `database/seeders/IntegrationTestPartnerSeeder.php`: город «Тестоград» с `levelion_city_id = 49` (соответствие городу в БД CRM), пользователь «Тестислав Партнёров» (`testislav.partner@superpart.ru`, пароль как у демо — `password`); в комментарии сидера напоминание подставлять `user_id` в CRM.
- Подключён вызов сидера в `database/seeders/DatabaseSeeder.php` после `UserSeeder`.
- В `app/Http/Controllers/Api/LevelionWebhookController.php` для webhook `order-completed` сумма начисления ограничена сверху 2500 (бизнес-правило на сервере, не на фронте).

## 02.04.2026 -- FTP: точечная выгрузка правок 419

- Добавлен `deploy-upload-2026-04-02-6.sh`: только `put` четырёх файлов (bootstrap/app.php, AppServiceProvider, login и app layout), без зеркала каталогов.

## 02.04.2026 -- FTP: скрипт выгрузки новых изменений

- Добавлен `deploy-upload-2026-04-02-5.sh`: зеркалирование `app`, `bootstrap`, `config`, `database`, `public`, `resources`, `routes`, `artisan` (без `vendor`/`storage`). Обновлены `scripts/deploy.sh`, `docs/DEPLOY.md`.

## 02.04.2026 -- Ошибка 419 при входе (CSRF / прокси)

- В `bootstrap/app.php`: доверие прокси `trustProxies(at: '*')` (типично для SSL на reg.ru); обработка `TokenMismatchException` — редирект на форму входа или `back()` с флешем `csrf_notice` вместо отдельной страницы 419.
- В `AppServiceProvider`: `URL::forceScheme('https')`, если `APP_URL` начинается с `https://`.
- В `resources/views/auth/login.blade.php` и `layouts/app.blade.php`: вывод `csrf_notice`.
- В `deploy/regru/.env.example`: комментарий про `APP_URL` с https.

## 02.04.2026 -- FTP: только миграции и сидеры (тестовые данные)

- Добавлен `deploy-upload-2026-04-02-4.sh`: зеркалирование только `database/migrations` и `database/seeders`; в конце подсказка по `migrate` / `migrate:fresh --seed` и предупреждение про непустую БД.

## 02.04.2026 -- FTP: быстрый скрипт без vendor/storage

- Добавлен `deploy-upload-2026-04-02-3.sh`: выгрузка только `app`, `config`, `database`, `public`, `resources`, `routes`, `artisan` (без `vendor` и `storage`). Полные скрипты `-2` и `2026-04-02` явно помечены в шапках как «полная синхронизация».
- Обновлены `docs/DEPLOY.md`, `scripts/deploy.sh`, `.cursor/rules/superpart-general.mdc`.

## 02.04.2026 -- FTP: домашний каталог = корень Laravel, без cd в скрипте

- В `deploy-upload-2026-04-02.sh` и `deploy-upload-2026-04-02-2.sh` убран `cd www/SuperPart.ru`: у пользователя FTP домашняя директория уже `/www/SuperPart.ru`.
- Обновлены `docs/DEPLOY.md` и `.cursor/rules/superpart-general.mdc`.

## 02.04.2026 -- FTP-скрипты: учётные данные в файле, пароль только в команде

- В `deploy-upload-2026-04-02.sh` и `deploy-upload-2026-04-02-2.sh` зашиты хост `31.31.197.5`, порт `21`, пользователь `u3398705_cursor_testir`, каталог `www/SuperPart.ru`; обязательна только переменная `FTP_PASS` при запуске; URL для lftp `ftp://хост:порт`.
- В `.cursor/rules/superpart-general.mdc` добавлен раздел «FTP-скрипты deploy-upload-*.sh» с этим правилом и напоминанием про новый файл на каждый деплой.
- В `docs/DEPLOY.md` обновлено описание запуска выгрузки.

## 02.04.2026 -- Продакшен-контур: правила Cursor, FTP-синк, документация

- В `.cursor/rules/superpart-general.mdc` добавлен блок «Рабочий контур и продакшен»: основная среда — сервер reg.ru, правки на сервере в приоритете, локальная копия для синхронизации; данные — MySQL на сервере; напоминание про `php83`. Обновлены стек (PHP 8.3+, Laravel 13) и формулировка про источник данных.
- Добавлен скрипт `deploy-upload-2026-04-02-2.sh` — повторная FTP-выгрузка на прод (`FTP_REMOTE_ROOT` по умолчанию `www/SuperPart.ru`), с подсказками про `npm run build` и `composer install --no-dev`.
- В `docs/DEPLOY.md` в начале указан режим «приложение в продакшене».
- В `docs/prompts/00_БАЗОВЫЙ_КОНТЕКСТ.md` добавлен подпункт «Рабочий контур», обновлена таблица стека и принцип про БД.

## 02.04.2026 -- Сидеры: 5 пользователей, 50 заказов, источники и сотрудники

- Обновлены `database/seeders/UserSeeder.php` (пользователи `test@`, `admin@`, `partner1@`…`partner3@`, пароль `password`, `theme` dark), `SourceSeeder.php` (5 источников на каждого пользователя с суффиксом логина), `EmployeeSeeder.php` (по 3 сотрудника на пользователя), `OrderSeeder.php` (ровно 50 заказов, распределение по пользователям по кругу).
- Добавлен `docs/ДЕМО_ДАННЫЕ.md` — таблица логинов и краткое описание демо-данных.

## 02.04.2026 -- DEPLOY: раздел про HTTP 500

- В `docs/DEPLOY.md` добавлен §9: отладка 500 (`tail storage/logs/laravel.log`), типичные причины (БД/миграции, кэш конфига, права, PHP 8.3 для веба, `APP_KEY`), временное включение `APP_DEBUG`.

## 02.04.2026 -- Документация деплоя reg.ru: «наш опыт»

- Добавлен файл `docs/DEPLOY_REG_RU_НАШ_ОПЫТ.md`: шаблон с двумя схемами размещения (классика Laravel с `public` и вариант `www` + папка приложения по аналогии с Levelion CRM), таблица под SSH (`php83` и т.д.), блок «заполнить после успешного деплоя», чек-лист перед отладкой; отсылка к `Levelion_KZ/docs/ДЕПЛОЙ_РЕГРУ.md` для второй схемы.
- В `docs/DEPLOY.md` в начале добавлена ссылка на этот файл.

## 02.04.2026 -- DEPLOY: заглушка reg.ru «Сайт размещен некорректно»

- В `docs/DEPLOY.md` добавлен раздел 8: объяснение фирменной страницы reg.ru при неверном каталоге сайта; для Laravel корень в панели должен указывать на **`…/public`**, не на корень с `artisan`; проверка наличия `public/index.php` и `.htaccess` по SSH; напоминание про PHP 8.3+ для веба.
- В разделе 2 того же файла уточнено требование к пути с суффиксом `/public` и отсылка к разделу 8.

## 02.04.2026 -- Деплой: команды на сервере через php83 (reg.ru)

- В `docs/DEPLOY.md` добавлено пояснение про SSH на reg.ru: проект требует PHP ≥ 8.3, в консоли часто нужно вызывать **`php83`**, а не `php`; команды после выгрузки приведены с `php83 artisan …`, с оговоркой заменить на `php`, если в SSH уже 8.3+.
- В `docs/DEPLOY.md` добавлен блок про ошибку с чужим путём к `storage/logs` после выгрузки с dev-машины (кэш конфига) и команды очистки (`rm` кэш-файлов в `bootstrap/cache`, `config:clear`, `optimize`).
- Обновлены выводимые подсказки в `scripts/deploy.sh` и `deploy-upload-2026-04-02.sh` под тот же формат (`php83 artisan …`).
- В шапке комментария `deploy/regru/env.production-testir.space` добавлена отсылка к `docs/DEPLOY.md` про `php83`.

## 02.04.2026 -- Первый деплой: скрипты подготовки, FTP, документация reg.ru

- Добавлен скрипт локальной подготовки к продакшену `scripts/deploy.sh`: полный tar.gz-снимок dev в `backups/superpart-dev-snapshot-*.tar.gz` (без каталога `backups`), `npm ci`/`npm run build`, `composer install --no-dev --optimize-autoloader`, `php artisan optimize`; в конце выводится блок команд для сервера (`migrate --force`, `storage:link`, `optimize`, права на `storage` и `bootstrap/cache`) и подсказка `optimize:clear` для возврата к разработке.
- Добавлена инструкция `docs/DEPLOY.md`: структура каталогов на reg.ru (DocumentRoot на `public`), `.env`, загрузка по FTP, чек-лист, восстановление из снимка.
- Добавлен каталог `deploy/regru/`: эталонные `index.php` и `.htaccess` (как в `public/`), шаблон `deploy/regru/.env.example` для production (MySQL, `APP_DEBUG=false`, локали ru).
- Добавлен скрипт выгрузки по FTP `deploy-upload-2026-04-02.sh` (lftp, `mirror` каталогов `app`, `bootstrap`, `config`, `database`, `public`, `resources`, `routes`, `vendor`, `storage` без `*.log`, файлы `artisan`, `composer.json`, `composer.lock`; переменные `FTP_HOST`, `FTP_USER`, `FTP_PASS`, опционально `FTP_REMOTE_ROOT`); в конце блок команд для сервера. Не выгружаются `docs/`, `Import/`, `.cursor/`, `node_modules/`, `tests/`, `backups/`.

## 02.04.2026 -- Кнопка поиска в текстовых фильтрах таблиц

- Добавлен компонент `resources/views/components/ui/filter-input.blade.php` (`<x-ui.filter-input>`): поле ввода с иконкой «лупа» справа для явного применения фильтра (не только по Enter).
- Текстовые фильтры в строках таблиц переведены на `x-ui.filter-input` в представлениях:
  - `resources/views/dashboard/index.blade.php`;
  - `resources/views/orders/index.blade.php`;
  - `resources/views/orders/unprocessed.blade.php`;
  - `resources/views/reports/orders.blade.php`;
  - `resources/views/transactions/index.blade.php`;
  - `resources/views/withdrawals/index.blade.php`;
  - `resources/views/reviews/index.blade.php`;
  - `resources/views/cities/index.blade.php` (поля «Город» и «ID»);
  - `resources/views/components/data-table.blade.php` (текстовый тип фильтра).
- В `resources/js/app.js` добавлена `initFilterSearchButtons()`: клик по `.filter-search-btn` отправляет форму (`requestSubmit`) или вызывает `filterTable()` для клиентских таблиц (`[data-table]`, таблицы с `tr[data-row]`); для страницы городов вызывается `window.superpartCitiesApplyFilters()` (назначается в скрипте `cities/index.blade.php`).
- В `bindCloneEvents` внутри `initStickyTables()` добавлена обработка клика по лупе в клоне «липкой» шапки: синхронизация с полем в оригинальном `thead` и то же действие, что у основной кнопки.

## 02.04.2026 -- Исправление sticky-заголовка таблиц

- Исправлен механизм закрепления заголовков таблиц при вертикальной прокрутке:
  - в `resources/js/app.js` добавлена функция `initStickyTables()`, которая автоматически назначает класс `table-scroll` всем контейнерам `.overflow-x-auto`, если внутри есть `<table>`;
  - инициализация `initStickyTables()` добавлена в общий `DOMContentLoaded` рядом с остальными инициализаторами.
- В `resources/css/app.css` переписаны стили закрепления:
  - глобальные правила `table thead ...` заменены на scoped-правила для `.table-scroll`;
  - добавлены `overflow-y: auto` и `max-height: 70vh` для контейнера;
  - закрепление сделано для первой строки заголовка (`top: 0`) и второй строки фильтров (`top: 44px`).
- Это устраняет ситуацию, когда sticky не срабатывал из-за отсутствия вертикального скролл-контейнера и/или применения стилей слишком широко.

## 02.04.2026 -- Дополнительные правки: тема в настройках, sticky-заголовки, перевод вида, пагинация в UI Kit

- Усилена логика переключения темы на странице `Настройки` (`resources/views/settings/index.blade.php`): добавлен прямой fallback‑механизм смены темы и синхронизации через `PATCH /settings/theme`, если глобальная функция темы недоступна.
- Добавлено глобальное поведение закрепления заголовков таблиц при вертикальной прокрутке в `resources/css/app.css`:
  - `thead th` закрепляются сверху;
  - в таблицах с двумя строками заголовка (`строка фильтров`) вторая строка также закрепляется ниже первой.
- Исправлен перевод типа заявки `first_time` в представлениях:
  - `resources/views/orders/index.blade.php`;
  - `resources/views/orders/unprocessed.blade.php`;
  - `resources/views/orders/show.blade.php`;
  - `resources/views/dashboard/index.blade.php`.
- Расширен компонент `resources/views/components/data-table.blade.php`: добавлен опциональный параметр `paginator` и вывод `links()` при наличии пагинации.
- В `resources/views/ui-kit/index.blade.php` добавлена демонстрация реальной пагинации для `x-data-table` (с `LengthAwarePaginator`, `ui_page`, 5 строк на страницу).

## 02.04.2026 -- Точечные исправления: тема в настройках, экспорт начисления, данные для пагинации

- Исправлена логика переключения темы на странице `Настройки` (`/settings`): в `resources/js/app.js` добавлен единый глобальный метод `window.superpartSetTheme()`, который применяет тему и синхронизирует её с сервером.
- Обновлён скрипт страницы `resources/views/settings/index.blade.php`: кнопки выбора темы теперь используют `window.superpartSetTheme()`; добавлен fallback на событие `superpart:set-theme`.
- Исправлен формат поля `Начисление` в экспорте заказов (`app/Exports/OrdersExport.php`): вместо строки вида `1 500 Р` теперь выгружается числовое значение без суффикса валюты.
- Для проверки пагинации добавлены ещё 10 заказов через `php artisan tinker` (итого заказов: 35).

## 02.04.2026 -- Страница настроек, экспорт заявок и пагинация списков

### Настройки пользователя (`/settings`)
- Добавлены маршруты `GET /settings`, `PATCH /settings/profile`, `PATCH /settings/password`, `PATCH /settings/theme` в `routes/web.php`.
- Создан контроллер `app/Http/Controllers/SettingsController.php` с методами просмотра настроек, обновления профиля, смены пароля и сохранения темы.
- Добавлена миграция `database/migrations/2026_04_02_000001_add_theme_to_users_table.php` с полем `theme` (`dark`/`light`) в таблице `users`.
- Обновлена модель `app/Models/User.php`: поле `theme` добавлено в `$fillable`.
- Создана страница `resources/views/settings/index.blade.php` с тремя блоками: профиль, смена пароля, выбор темы.
- В `resources/views/layouts/app.blade.php` класс темы на `<html>` теперь берётся из профиля пользователя.
- В `resources/js/app.js` обновлена логика темы: переключение сохраняется в `localStorage` и синхронизируется с сервером через `PATCH /settings/theme`; добавано событие `superpart:set-theme` для кнопок на странице настроек.

### Экспорт таблицы заявок (`/orders`)
- Добавлена зависимость `openspout/openspout` в `composer.json` и `composer.lock` для генерации `CSV` и `XLSX`.
- Создан класс `app/Exports/OrdersExport.php` для формирования файлов экспорта (заголовки, форматирование дат/сумм, маппинг статусов и видов работ).
- В `app/Http/Controllers/OrderController.php` добавлен метод `export(Request $request)` с поддержкой форматов `csv` и `xlsx`, с применением текущих фильтров/сортировки списка.
- В `routes/web.php` добавлен маршрут `GET /orders/export` (размещён перед `/orders/{id}`).
- В `resources/views/orders/index.blade.php` иконка экспорта заменена на выпадающее меню с вариантами `Excel (XLSX)` и `CSV`, в ссылки передаются текущие query‑параметры.

### Пагинация рабочих списков (по 25 строк)
- Переведены на серверную пагинацию контроллеры:
  - `app/Http/Controllers/OrderController.php` (`index`, `unprocessed`);
  - `app/Http/Controllers/TransactionController.php`;
  - `app/Http/Controllers/WithdrawalController.php`;
  - `app/Http/Controllers/ReviewController.php`;
  - `app/Http/Controllers/EmployeeController.php`;
  - `app/Http/Controllers/SourceController.php`;
  - `app/Http/Controllers/CityController.php`;
  - `app/Http/Controllers/WorkTypeController.php`.
- Для списков с фильтрами добавлено сохранение query‑параметров между страницами через `withQueryString()`.
- В `ReviewController` изменено получение списка городов: список формируется из полного отфильтрованного запроса до пагинации, чтобы фильтр городов не ограничивался текущей страницей.
- В шаблоны списков добавлены блоки пагинации:
  - `resources/views/orders/index.blade.php`;
  - `resources/views/orders/unprocessed.blade.php`;
  - `resources/views/transactions/index.blade.php`;
  - `resources/views/withdrawals/index.blade.php`;
  - `resources/views/reviews/index.blade.php`;
  - `resources/views/employees/index.blade.php`;
  - `resources/views/sources/index.blade.php`;
  - `resources/views/cities/index.blade.php`;
  - `resources/views/work-types/index.blade.php`.
- В таблицах заявок (`orders/index`, `orders/unprocessed`) исправлена сквозная нумерация строк с учётом текущей страницы.

### Шаблон пагинации Laravel
- Выполнена публикация шаблонов пагинации командой `php artisan vendor:publish --tag=laravel-pagination`.
- Обновлён `resources/views/vendor/pagination/tailwind.blade.php`: стандартные серо‑белые классы заменены на семантические токены проекта (`bg-card`, `text-foreground`, `text-muted-foreground`, `border-border`, `bg-primary`, `text-primary-foreground`, `hover:bg-muted`).

## 02.04.2026 -- Дизайн-система в коде (токены, UI-примитивы, миграция шаблонов)

### Документация и правила
- Добавлен `docs/DESIGN_SYSTEM.md` — токены, семантические цвета, типографика, тени, состояния интерактивных элементов, чеклист для разработки.
- В `docs/prompts/00_БАЗОВЫЙ_КОНТЕКСТ.md` в раздел «Визуальный стиль» добавлена ссылка на `docs/DESIGN_SYSTEM.md`.
- Добавлено правило Cursor `.cursor/rules/superpart-design-system.mdc` (globs: `resources/views/**/*.blade.php`, `resources/css/**/*.css`).

### UI-примитивы Blade (`resources/views/components/ui/`)
- `button.blade.php` — варианты primary, secondary, ghost, destructive, outline; размеры sm, md, lg, icon; `tag` button | a, `href` для ссылок.
- `input.blade.php`, `select.blade.php`, `textarea.blade.php` — размеры sm (фильтры таблиц) и md (формы), флаг `error`.
- `label.blade.php` — подпись с опциональной звёздочкой обязательности.
- `form-group.blade.php` — лейбл + слот контрола + `@error` для поля.
- `card.blade.php` — обёртка карточки: padding none | sm | md | lg, опционально `shadow`.
- `alert.blade.php` — типы success, error, warning, info (часть через `chart-*` токены).
- `checkbox.blade.php` — чекбокс с подписью.

### Составные компоненты
- `components/status-badge.blade.php` — добавлены типы `review` (статусы new, resolved, not_resolved) и `employee` (active, inactive); для `withdrawal`/`in_work` без фона цвет текста заменён на `text-muted-foreground` вместо `text-gray-300`.
- `components/data-table.blade.php` — фильтры вынесены на `x-ui.input` / `x-ui.select`; внешняя обёртка на `x-ui.card` (padding none, shadow-sm).
- `components/date-filter.blade.php` — поля даты и селект на UI-примитивы; кнопка отправки на `x-ui.button` (primary, icon, rounded-full).
- `components/page-header.blade.php` — кнопка действия на `x-ui.button` (ссылка).

### Миграция представлений
- Формы: `orders/create.blade.php`, `sources/create.blade.php`, `employees/create.blade.php`, `reviews/create.blade.php`, `withdrawals/form.blade.php`, `auth/login.blade.php` переведены на `x-ui.card`, `x-ui.form-group`, `x-ui.input` / `select` / `textarea` / `checkbox` / `button` / `alert`.
- Списки и таблицы: обновлены фильтры и обёртки карточек в `orders/index.blade.php`, `orders/unprocessed.blade.php`, `orders/show.blade.php`, `dashboard/index.blade.php`, `transactions/index.blade.php`, `withdrawals/index.blade.php`, `reviews/index.blade.php`, `reports/orders.blade.php`, `reports/cities.blade.php`, `reports/work-types.blade.php`, `reports/statistics.blade.php`, `reports/reviews.blade.php`, `cities/index.blade.php`, `work-types/index.blade.php`, `sources/index.blade.php`, `employees/index.blade.php`.
- `partials/navbar.blade.php` — кнопки «Создать» и переключатель темы на `x-ui.button`.

### Витрина и маршрут
- Добавлен шаблон `resources/views/ui-kit/index.blade.php` (палитра, типографика, кнопки, поля, алерты, карточки, бейджи, пример `x-data-table`, хлебные крошки, баланс, вкладки).
- Маршрут `GET /ui-kit` (имя `ui-kit`) зарегистрирован только при `App::isLocal()` в `routes/web.php`.

### Сборка
- Выполнена `npm run build` (Vite), CSS собран без ошибок.

## 31.03.2026 -- Старт проекта

### Выполнено
- Изучено текстовое ТЗ (`Import/ТЗ`)
- Просмотрены все 20 скриншотов сайта-конкурента (partner-lead-centre.ru)
- Определён стек технологий на основе Levelion_dev (Laravel 12 + Blade + Tailwind CSS 4 + Vite + MySQL)
- Составлен детальный план разработки (8 этапов, 17 страниц)
- Согласованы корректировки с заказчиком:
  - Убран баннер бонусов с главной
  - Убран блок "Новости" с главной
  - "Сервис" и "Новости" убраны из навбара
  - Отзывы вынесены в отдельный пункт меню
  - Добавлена страница "Карточка заказа"
  - "Отчёт по видам услуг" = "Отчёт по видам работ" (один пункт)
  - Данные -- локальная MySQL без моков
- Создана файловая структура документации (`docs/`)

### Этап 0: Инициализация Laravel-проекта
- Установлен Laravel 13 (последняя доступная версия) во временную папку и перенесён в `superpart.ru` без затирания `docs/`, `Import/`, `.cursor/`
- Установлен Node.js 22 LTS через nvm (не был установлен в системе)
- Установлены npm-зависимости: Vite 8, laravel-vite-plugin, Tailwind CSS 4, @tailwindcss/vite
- `vite.config.js` настроен (laravel-vite-plugin + @tailwindcss/vite) -- пришёл корректным из коробки
- `resources/css/app.css` -- Tailwind v4 CSS-first подход (`@import 'tailwindcss'`)
- `.env` настроен: APP_NAME=SuperPart, APP_URL=http://localhost:8010, локаль ru, MySQL
- Создана база данных `superpart` в MySQL (utf8mb4_unicode_ci)
- Создан пользователь MySQL `superpart` с правами на базу
- Стандартные миграции Laravel выполнены (users, cache, jobs)
- Проверен запуск: `php artisan serve --port=8010` отдаёт 200, Vite dev-сервер стартует

### Этап 2a: Миграции базы данных
- Добавлено поле `balance` (decimal 12,2) в таблицу `users`
- Создана таблица `cities` (города: название, доступность, загруженность)
- Создана таблица `work_types` (виды работ: название, описание, профильность)
- Создана таблица `sources` (источники: привязка к партнёру, название, комментарий, отзыв)
- Создана таблица `employees` (сотрудники: привязка к партнёру, email, имя, статус, последний визит)
- Создана таблица `orders` (заявки: центральная таблица с 8 статусами, связи с городом, источником, видом работ, сотрудником)
- Создана таблица `transactions` (начисления: сумма, тип операции, остаток баланса, связь с заявкой)
- Создана таблица `withdrawal_requests` (заявки на вывод: статус, сумма, карта, реквизиты, комментарий)
- Создана таблица `withdrawal_request_items` (позиции заявки на вывод: связь с транзакцией, заявкой, городом)
- Создана таблица `reviews` (отзывы: статус, ссылка, тип, город, дата закрытия)
- Все внешние ключи (foreign keys) настроены с правильным порядком зависимостей
- Проверка `php artisan migrate:fresh` -- 13 миграций выполнены без ошибок

### Этап 2b: Eloquent-модели
- Модифицирована модель `User` -- добавлено поле `balance` в fillable/casts, добавлены 6 связей (employees, orders, transactions, withdrawalRequests, sources, reviews)
- Создана модель `City` -- fillable (name, is_available, load_percentage), связь orders
- Создана модель `WorkType` -- fillable (name, description, is_profile), связь orders
- Создана модель `Source` -- fillable (user_id, name, comment, has_review), связи user, orders
- Создана модель `Employee` -- fillable (user_id, email, name, status, last_visit_at), связи user, orders
- Создана модель `Order` -- fillable (16 полей), 5 связей belongsTo (user, city, source, workType, employee), 2 связи hasMany (transactions, reviews)
- Создана модель `Transaction` -- fillable (user_id, order_id, amount, operation_type, balance_after, completed_at), связи user, order
- Создана модель `WithdrawalRequest` -- fillable (user_id, status, total_amount, bank_card, requisites, comment), связи user, items
- Создана модель `WithdrawalRequestItem` -- fillable (7 полей), связи withdrawalRequest, transaction, order, city
- Создана модель `Review` -- fillable (user_id, order_id, status, review_url, review_type, city_id, closed_at), связи user, order, city
- Все модели используют единый стиль: классические $fillable/$hidden, метод casts(), типизированные связи через return type HasMany/BelongsTo
- Проверка через tinker -- все 10 моделей загружаются, SQL-запросы указывают на правильные таблицы

### Этап 3: Аутентификация
- Создан `AuthController` с 5 методами: showLogin, login, showRegister, register, logout
- Реализована валидация: вход (email + пароль), регистрация (имя, email, пароль с подтверждением, min:8, unique:users)
- Создан layout `layouts/auth.blade.php` -- тёмный фон (#1a1a2e), центрированная карточка
- Создана страница входа `auth/login.blade.php` -- форма email/пароль, чекбокс "Запомнить меня", ссылка на регистрацию
- Создана страница регистрации `auth/register.blade.php` -- форма имя/email/пароль/подтверждение, ссылка на вход
- Обе страницы в тёмной теме с зелёными кнопками (#4caf50), отображают ошибки валидации
- Настроены маршруты: GET/POST /login, GET/POST /register (middleware guest), POST /logout (middleware auth)
- Все остальные маршруты защищены middleware `auth` -- незалогиненных перенаправляет на /login
- Middleware `guest` на auth-страницах -- залогиненных перенаправляет на /
- Настроены редиректы в `bootstrap/app.php`: гостей → /login, пользователей → /
- Проверка `php artisan route:list` -- 6 маршрутов приложения + системные зарегистрированы корректно

### Этап 5: Layout и навигационная панель
- Создан основной layout `layouts/app.blade.php` -- тёмная тема (#1a1a2e), подключение Vite, секции breadcrumbs/content
- Создан `partials/navbar.blade.php` -- горизонтальная навигация с тёмным фоном (#2d2d3f), sticky позиционирование
- В навбаре реализованы 5 выпадающих меню: Заявки (2 пункта), Начисления (2), Отчёты (4), Справочники (4), Отзывы (2)
- Справа в навбаре: бейдж баланса с зелёной рамкой, зелёная кнопка "Создать", меню пользователя (Имя, ID) с Настройки/Выход
- Создан Blade-компонент `x-dropdown-menu` -- переиспользуемое выпадающее меню (принимает title и items)
- Создан Blade-компонент `x-balance-badge` -- отображение баланса в зелёной рамке, формат "XX XXX Р"
- Создан Blade-компонент `x-breadcrumbs` -- хлебные крошки с разделителями, последний элемент полужирный
- Под навбаром: строка с хлебными крошками (слева) и дублем баланса (справа)
- Добавлен JS для выпадающих меню: открытие по hover и клику, закрытие при уходе мыши / клике вне
- Добавлены CSS: стили переходов для dropdown, стилизация скроллбара для тёмной темы
- Создана временная главная страница `home.blade.php` с использованием нового layout
- Маршрут `/` обновлён: теперь отдаёт `home` вместо `welcome`
- Создан тестовый пользователь (Животков Иван, баланс 78 250 Р) для проверки
- Проверка: шаблон рендерится без ошибок (11727 символов HTML)

### Этап 6: Переиспользуемые Blade-компоненты
- Создан компонент `x-status-badge` -- цветные бейджи статусов для заявок (8 статусов), транзакций (1 статус), вывода средств (3 статуса). Маппинг цветов по скриншотам: зелёный/красный/синий/оранжевый/серый
- Создан компонент `x-tabs` -- вкладки-фильтры с подсчётом количества. Активная вкладка с белым подчёркиванием, неактивные серые. Работают как ссылки с ?status=
- Создан компонент `x-page-header` -- заголовок страницы (крупный белый текст слева) с опциональной зелёной кнопкой справа (например "Создать")
- Создан компонент `x-date-filter` -- блок фильтрации по датам. 2 основных поля (дата от/по), опциональные "закрыто от/по" и "вид работ". Зелёная круглая кнопка применения, крестик для сброса. Тёмная тема
- Создан компонент `x-data-table` -- универсальная таблица данных. Заголовки с сортировкой, отдельная строка фильтров под заголовками (text/select), чередование строк, поддержка слота для кастомной отрисовки
- Добавлен JavaScript: клиентская сортировка таблиц (по числам и строкам), фильтрация по вводу в полях, сброс дат в фильтре
- Добавлены CSS-стили: стилизация полей даты для тёмной темы (color-scheme: dark), индикаторы направления сортировки
- Сборка Vite проходит без ошибок
- Итого 8 Blade-компонентов: breadcrumbs, balance-badge, dropdown-menu (этап 5) + status-badge, tabs, page-header, date-filter, data-table (этап 6)

### Этап 7: Главная страница "Партнёр"
- Создан `DashboardController` с методом `index` -- получает статистику заработка и список заказов с фильтрами
- Статистика заработка: подсчёт сумм из таблицы `transactions` (operation_type=charge) за 5 периодов -- сегодня, вчера, эта неделя, этот месяц, прошлый месяц
- Серверная фильтрация заказов по 7 параметрам: ID заявки, город, статус, вид, источник, имя клиента, телефон
- Серверная сортировка по любому столбцу (по умолчанию -- по дате создания, новые сверху)
- Получение уникальных значений для фильтров-селектов (города, статусы, виды, источники) из данных текущего пользователя
- Создан шаблон `dashboard/index.blade.php` -- горизонтальный блок статистики (5 ячеек) + таблица заказов (14 столбцов)
- Блок статистики: подписи серым текстом ("СЕГОДНЯ", "ВЧЕРА" и т.д.), суммы белым крупным текстом, разделены вертикальными линиями
- Таблица заказов: порядковый номер, ID, иконка открытия в новой вкладке, непрофильность (✓), город, статус (цветной бейдж), вид, источник, время заявки, имя, телефон, дата создания, начисление, сотрудник
- Строка фильтров под заголовками: текстовый поиск (ID, имя, телефон) + выпадающие списки (город, статус, вид, источник)
- Фильтры-селекты применяются при изменении, текстовые -- по Enter
- Клик по строке таблицы -- переход на карточку заказа `/orders/{id}`
- Клик по иконке -- открытие карточки заказа в новой вкладке
- Маршрут `/` обновлён с замыкания на `DashboardController@index`
- Удалён временный шаблон `home.blade.php`
- Добавлен `@stack('scripts')` в layout для поддержки инлайн-скриптов на страницах
- Баннер бонусов, блок лидов/конверсий и колонка новостей -- отложены (по согласованию)

### Этап 8: Карточка заказа (`/orders/{id}`)
- Создан `OrderController` с методом `show($id)` -- загрузка заказа с eager load (city, source, workType, employee) и проверкой принадлежности текущему пользователю
- Добавлен маршрут `GET /orders/{id}` → `OrderController@show` (name: `orders.show`)
- Создан шаблон `resources/views/orders/show.blade.php` -- карточка заказа в тёмной теме
- Хлебные крошки: "Главная > Список заявок > Заявка №{id}"
- Информация о заявке в двухколоночном блоке:
  - Левая колонка: ID, статус (цветной бейдж), вид (Впервые/Гарантия/Повтор), профильность, город, населённый пункт/адрес, вид работ
  - Правая колонка: имя клиента, телефон, источник, сотрудник (или "Переадресация"), время заявки, дата создания, дата закрытия (если есть)
- Отдельный блок "Сумма начисления" -- крупным шрифтом, формат "XX XXX Р"
- Кнопка "Назад к списку" внизу страницы
- Клик по строке заказа на главной (`data-href="/orders/{id}"`) ведёт на эту страницу
- Используются существующие компоненты: `x-breadcrumbs`, `x-page-header`, `x-status-badge`, `x-balance-badge`

### Этап 9: Страница "Список заявок" (`/orders`)
- Добавлен метод `index` в `OrderController` -- получение заявок с фильтрацией по статусу из вкладок + фильтры из столбцов таблицы
- Подсчёт количества заявок по каждому из 8 статусов для отображения в вкладках
- Серверная фильтрация по 7 параметрам: ID, город, статус, вид, источник, имя клиента, телефон
- Серверная сортировка по любому столбцу (по умолчанию -- по дате создания, новые сверху)
- Создан шаблон `resources/views/orders/index.blade.php`:
  - Хлебные крошки "Главная > Список заявок" + баланс справа
  - Заголовок "Список заявок"
  - Вкладки статусов (компонент `x-tabs`): Все заявки, В работе, На уточнении, Не оформлена, Ожидает, Ожидает выплаты, Отказ, Отказ Непрофиль, Отмена -- с подсчётом количества
  - Кнопка "Создать" (зелёная) + иконка экспорта справа на уровне вкладок
  - Таблица заявок: 15 столбцов (порядковый номер, ID, иконка, НПр, город, статус, вид, нас.пункт/адрес, источник, время заявки, имя, телефон, дата создания, начисление, сотрудник)
  - Строка фильтров под заголовками: текстовый поиск (ID, имя, телефон) + селекты (город, статус, вид, источник)
  - Фильтры-селекты применяются при изменении, текстовые -- по Enter
  - Клик по строке -- переход на карточку заказа `/orders/{id}`
  - Клик по иконке -- открытие карточки в новой вкладке
  - Переключение вкладок -- через URL-параметр `?status=`
- Добавлен маршрут `GET /orders` → `OrderController@index` (name: `orders.index`)
- Обновлены хлебные крошки в карточке заказа -- ссылка "Список заявок" теперь ведёт на `/orders`
- Вынесены константы `STATUS_LABELS` и `TYPE_LABELS` в контроллер для переиспользования
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-tabs`, `x-status-badge`, `x-balance-badge`

### Этап 10: Страница "Не оформленные" (`/orders/unprocessed`)
- Добавлен метод `unprocessed()` в `OrderController` -- аналог `index()`, но с жёстким фильтром `status = 'not_processed'` и без вкладок статусов
- Создан шаблон `resources/views/orders/unprocessed.blade.php`:
  - Хлебные крошки "Главная > Не оформленные" + баланс справа
  - Заголовок "Не оформленные"
  - Таблица заявок: те же 15 столбцов что на "Список заявок" (порядковый номер, ID, иконка, НПр, город, статус, вид, нас.пункт/адрес, источник, время заявки, имя, телефон, дата создания, начисление, сотрудник)
  - Строка фильтров под заголовками: текстовый поиск (ID, имя, телефон) + селекты (город, статус, вид, источник)
  - Вкладок статусов нет (в отличие от "Список заявок") -- показываются только заявки со статусом "Не оформлена"
  - Фильтры-селекты применяются при изменении, текстовые -- по Enter
  - Клик по строке -- переход на карточку заказа `/orders/{id}`
  - Клик по иконке -- открытие карточки в новой вкладке
- Добавлен маршрут `GET /orders/unprocessed` → `OrderController@unprocessed` (name: `orders.unprocessed`)
- Маршрут размещён перед `/orders/{id}` чтобы Laravel не воспринимал "unprocessed" как ID
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-status-badge`, `x-balance-badge`

### Этап 11: Страница "Список начислений" (`/transactions`)
- Создан `TransactionController` с методом `index` -- получение транзакций текущего пользователя с фильтрацией по датам и поиском по ID транзакции
- Серверная фильтрация: по дате от/по (через `completed_at`), по ID транзакции (LIKE-поиск)
- Eager load связи `order` для отображения номера заявки в столбце "Заявка"
- Сортировка по `completed_at` DESC (новые транзакции сверху)
- Создан шаблон `resources/views/transactions/index.blade.php`:
  - Хлебные крошки "Главная > Список начислений" + баланс справа
  - Заголовок "Список начислений"
  - Баланс внутри блока таблицы (текст "Баланс: XX XXX Р" над фильтром, как на скриншоте 6.png)
  - Фильтр по датам (компонент `x-date-filter`) -- поля "Дата от" и "Дата по" с зелёной кнопкой применения
  - Таблица начислений: 6 столбцов (ID транзакции, Выполнено, Заявка, Сумма, Операция, Остаток)
  - Строка фильтров под заголовками: текстовый поиск по ID транзакции (применяется по Enter)
  - Столбец "Операция" -- цветной бейдж через `x-status-badge` (type="transaction")
  - Суммы и остатки выровнены вправо, формат "XX XXX Р"
  - Столбец "Заявка" -- текст "Заявка клиента №{order_id}"
- Добавлен маршрут `GET /transactions` → `TransactionController@index` (name: `transactions.index`)
- Дополнен компонент `x-status-badge` (type="transaction") -- добавлены типы операций `withdrawal` (красный, "Списание") и `correction` (оранжевый, "Корректировка") к существующему `charge` (зелёный, "Начисление")
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-date-filter`, `x-status-badge`, `x-balance-badge`

### Дополнительно: Страница создания заявки (`/orders/create`)
- Добавлен маршрут `GET /orders/create` → `OrderController@create` и `POST /orders` → `OrderController@store`
- Метод `create()` загружает списки городов, видов работ, источников и сотрудников текущего пользователя
- Метод `store()` выполняет валидацию, создаёт заявку со статусом `not_processed`, заполняет время заявки и локальное время создания, определяет непрофильность по выбранному виду работ
- Создан шаблон `resources/views/orders/create.blade.php` — форма в тёмной теме в две колонки (поля города, вида работ, типа, непрофильности, клиента, телефона, источника, сотрудника, населённого пункта и адреса)
- Кнопка "Создать" на странице списка заявок (`/orders`) теперь ведёт на `/orders/create`

### Этап 12: Страница "Заявки на вывод д/с" (`/withdrawals`)
- Создан `WithdrawalController` с методом `index` — получение заявок на вывод текущего пользователя с фильтрацией по датам, поиском по ID и фильтрацией по статусу
- Подсчёт количества заявок по статусам для вкладок: Все заявки, В работе, Выполнен
- Серверная фильтрация: по дате от/по (через `created_at`), по ID (LIKE-поиск), по статусу (через выпадающий список в таблице)
- Метод-заглушка `show($id)` для будущей детальной страницы заявки (этап 13)
- Создан шаблон `resources/views/withdrawals/index.blade.php`:
  - Хлебные крошки "Главная > Заявки на вывод д/с" + баланс справа
  - Заголовок "Список заявок на вывод денежных средств"
  - Фильтр по датам (компонент `x-date-filter`) — поля "Дата от" и "Дата по" с зелёной кнопкой применения
  - Вкладки статусов (компонент `x-tabs`): Все заявки, В работе, Выполнен — с подсчётом количества
  - Таблица: 4 столбца (ID, Создание, Статус, Сумма)
  - Строка фильтров под заголовками: текстовый поиск по ID (применяется по Enter) + выпадающий список статусов (применяется при изменении)
  - Столбец "Статус" — бейдж через `x-status-badge` (type="withdrawal")
  - Клик по строке — переход на `/withdrawals/{id}`
- Добавлены маршруты `GET /withdrawals` и `GET /withdrawals/{id}` → `WithdrawalController`
- Создано 3 тестовых заявки на вывод (В работе, Выполнен, Отклонено) для проверки
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-date-filter`, `x-tabs`, `x-status-badge`, `x-balance-badge`

### Этап 13: Страница "Заявка на выплату д/с" (создание и просмотр)
- Добавлены маршруты `GET /withdrawals/create` → `WithdrawalController@create` и `POST /withdrawals` → `WithdrawalController@store`
- Реализован метод `create()` — получает все начисления (транзакции с типом `charge`), которые ещё не включены в другие заявки на вывод, eager load связей order.city
- Реализован метод `store()` — валидация данных (реквизиты обязательны, минимум 1 выбранная транзакция), создание withdrawal_request + withdrawal_request_items в DB-транзакции, подсчёт общей суммы, перенаправление на страницу просмотра
- Реализован метод `show($id)` — загрузка заявки с items, транзакциями и городами, проверка принадлежности текущему пользователю
- Создан единый шаблон `resources/views/withdrawals/form.blade.php` — используется для создания и просмотра (параметр `mode`: create/show)
- Страница создания:
  - Хлебные крошки "Главная > Заявки на вывод д/с > Заявка на вывод д/с"
  - Заголовок "Заявка на выплату д/с"
  - Зелёная кнопка "Подтвердить оплату" вверху формы
  - Поле "Банковская карта" (select с placeholder)
  - Поле "Реквизиты для вывода" (textarea) с красным предупреждением "Указывайте ФИО получателя" / "Обязательно указывайте банк"
  - Поле "Комментарий" (textarea, необязательное)
  - Таблица начислений с чекбоксами: №, галочка выбора, ID начисления, ID заявки, сумма, город, дата создания, статус "Ожидает выплаты"
  - Чекбокс "выбрать все" в заголовке таблицы
- Страница просмотра:
  - Заголовок с номером и статусом: "Заявка на выплату д/с №{id} - {Статус}"
  - Все поля в режиме только для чтения (без редактирования)
  - В таблице вместо чекбоксов — зелёные галочки (неактивные)
  - Кнопка "Подтвердить оплату" скрыта
- Отображение ошибок валидации и сообщений об успехе
- JavaScript: логика "выбрать все" / "снять все" с поддержкой indeterminate состояния
- Кнопка "Создать" в навбаре теперь ведёт на `/withdrawals/create` (маршрут зарегистрирован)
- Сверка со скриншотом 8.png: визуально соответствует эталону
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-balance-badge`

### Этап 14: Страница "Отчёт по заявкам" (`/reports/orders`)
- Создан `ReportController` с методом `orders(Request $request)` — получение заявок текущего пользователя с расширенной фильтрацией
- Вкладки "Все" / "Закрытые" — переключение через URL-параметр `?status=all|closed`, подсчёт количества для каждой вкладки
- Расширенный фильтр по датам (компонент `x-date-filter`): 4 поля дат (дата от/по, закрыто от/по) + выпадающий список "Вид работ" + зелёная кнопка применения
- Серверная фильтрация по столбцам: ID (текст), профиль (да/нет), город, статус, тип, источник, вид работ, имя, телефон, сотрудник — 10 фильтров
- Серверная сортировка по любому столбцу (по умолчанию — по дате создания, новые сверху)
- Eager load связей: city, source, workType, employee
- Создан шаблон `resources/views/reports/orders.blade.php`:
  - Хлебные крошки "Главная > Отчёт по заявкам" + баланс справа
  - Заголовок "Отчёт по заявкам"
  - Вкладки (компонент `x-tabs`): Все, Закрытые — с подсчётом количества
  - Расширенный фильтр по датам (компонент `x-date-filter`) с 4 датами и видом работ
  - Большая таблица: 15 столбцов (ID заявки, Профиль, Город, Статус, Тип, Нас.пункт/адрес, Источник, Вид работ, Время заявки, Имя, Телефон, Создано (лок), Закрыто (лок), Сумма начисления, Сотрудник)
  - Строка фильтров под заголовками: текстовый поиск (ID, имя, телефон) + селекты (профиль, город, статус, тип, источник, вид работ, сотрудник)
  - Фильтры-селекты применяются при изменении, текстовые — по Enter
  - Клик по строке — переход на карточку заказа `/orders/{id}`
  - Чередование строк (тёмнее/светлее), hover-эффект
- Добавлен маршрут `GET /reports/orders` → `ReportController@orders` (name: `reports.orders`)
- Сверка со скриншотом 10.png: структура соответствует эталону (вкладки, расширенный фильтр, большая таблица)
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-tabs`, `x-date-filter`, `x-status-badge`, `x-balance-badge`

### Этап 15: Страница "Отчёт по городам" (`/reports/cities`)
- Добавлен метод `cities(Request $request)` в `ReportController` — группировка заказов текущего пользователя по `city_id` с агрегацией COUNT и SUM(charge_amount)
- Фильтрация по датам: "Дата от" и "Дата по" (по полю `order_time`)
- Сводная таблица: 3 столбца (Город, Кол-во заявок, Всего оплачено)
- Строка "Итого" внизу таблицы — жирным шрифтом, с суммами по всем городам
- Суммы оплат отображаются только если > 0, формат: "1 974 Р" (пробел-разделитель, "Р")
- Создан шаблон `resources/views/reports/cities.blade.php`:
  - Хлебные крошки "Главная > Отчёт по городам" + баланс справа
  - Заголовок "Отчёт по городам"
  - Фильтр по датам (компонент `x-date-filter`)
  - Таблица с чередованием строк, строка итогов выделена фоном и жирным шрифтом
- Добавлен маршрут `GET /reports/cities` → `ReportController@cities` (name: `reports.cities`)
- Сверка со скриншотом 11.png: структура соответствует эталону (фильтр по датам, таблица городов, строка итогов)
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-date-filter`, `x-balance-badge`

### Этап 16: Страница "Отчёт по видам работ" (`/reports/work-types`)
- Добавлен метод `workTypes(Request $request)` в `ReportController` — группировка заказов текущего пользователя по `work_type_id` с агрегацией по статусам и суммам
- Агрегация: "Принято" (статусы in_work, clarification, not_processed, waiting, waiting_payment), "Не принято" (refusal, refusal_non_profile, cancelled), "Закрыто" (closed_local IS NOT NULL), "К оплате" (SUM charge_amount)
- Расширенный фильтр по 4 датам: "Дата от/по" (по order_time) и "Закрыто от/по" (по closed_local) — переиспользован приватный метод `applyDateFilters` (DRY)
- Сводная таблица: 5 столбцов (Вид работ, Принято, Не принято, Закрыто, К оплате)
- Строка "Итого" внизу таблицы — жирным шрифтом, с суммами по всем столбцам
- Суммы к оплате: формат "44 703 Р" (пробел-разделитель, "Р"), нулевые отображаются как "0 Р"
- Создан шаблон `resources/views/reports/work-types.blade.php`:
  - Хлебные крошки "Главная > Отчёт по видам работ" + баланс справа
  - Заголовок "Отчёт по видам работ"
  - Фильтр по 4 датам (компонент `x-date-filter` с `showClosedDates=true`)
  - Таблица с чередованием строк, строка итогов выделена фоном и жирным шрифтом
- Добавлен маршрут `GET /reports/work-types` → `ReportController@workTypes` (name: `reports.work-types`)
- Сверка со скриншотом 12.png: структура соответствует эталону (4 даты в фильтре, таблица видов работ, строка итогов)
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-date-filter`, `x-balance-badge`

### Этап 17: Страница "Статистика" (`/reports/statistics`)
- Добавлен метод `statistics(Request $request)` в `ReportController` — группировка заказов текущего пользователя по `source_id` с агрегацией
- Агрегация: "Принято" (статусы in_work, clarification, not_processed, waiting, waiting_payment), "Закрыто" (closed_local IS NOT NULL), "К оплате" (SUM charge_amount)
- Расширенный фильтр по 4 датам: "Дата от/по" (по order_time) и "Закрыто от/по" (по closed_local) — переиспользован приватный метод `applyDateFilters` (DRY)
- Сводная таблица: 4 столбца (Источник, Принято, Закрыто, К оплате)
- Строка "Итого" внизу таблицы — жирным шрифтом, с суммами по всем столбцам
- Суммы к оплате: формат "35 900 Р" (пробел-разделитель, "Р"), нулевые отображаются как "0 Р"
- Создан шаблон `resources/views/reports/statistics.blade.php`:
  - Хлебные крошки "Главная > Статистика" + баланс справа
  - Заголовок "Статистика"
  - Фильтр по 4 датам (компонент `x-date-filter` с `showClosedDates=true`)
  - Таблица с чередованием строк, строка итогов выделена фоном и жирным шрифтом
- Добавлен маршрут `GET /reports/statistics` → `ReportController@statistics` (name: `reports.statistics`)
- Сверка со скриншотом 13.png: структура соответствует эталону (4 даты в фильтре, таблица источников, строка итогов)
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-date-filter`, `x-balance-badge`

### Этап 18: Страница "Сотрудники" (`/employees`)
- Создан `EmployeeController` с 3 методами: `index` (список), `create` (форма), `store` (валидация + создание)
- Метод `index()` — получение сотрудников текущего пользователя, сортировка по ID (новые сверху)
- Метод `store()` — валидация (email обязательный + формат, имя обязательное), создание со статусом "active"
- Добавлены 3 маршрута: `GET /employees` (employees.index), `GET /employees/create` (employees.create), `POST /employees` (employees.store)
- Создан шаблон `resources/views/employees/index.blade.php`:
  - Хлебные крошки "Главная > Сотрудники" + баланс справа
  - Заголовок "Список сотрудников" + зелёная кнопка "Создать" справа (компонент `x-page-header`)
  - Таблица: 5 столбцов (ID, Эл. почта, Имя, Статус, Последний визит) — без фильтров (как на скриншоте 15.png)
  - Статус: зелёный "Активный" / красный "Неактивный"
  - Чередование строк (тёмнее/светлее), сообщение "Нет сотрудников" при пустом списке
  - Уведомление об успешном создании (зелёный flash-блок)
- Создан шаблон `resources/views/employees/create.blade.php`:
  - Хлебные крошки "Главная > Сотрудники > Создать сотрудника"
  - Заголовок "Создать сотрудника"
  - Форма в тёмном блоке: поля Email и Имя (обязательные), кнопки "Создать" и "Отмена"
  - Отображение ошибок валидации под каждым полем
- Сверка со скриншотом 15.png: структура соответствует эталону (заголовок + кнопка, таблица с 5 столбцами)
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-balance-badge`

### Этап 19: Страница "Источники" (`/sources`)
- Создан `SourceController` с 3 методами: `index` (список), `create` (форма), `store` (валидация + создание)
- Метод `index()` — получение источников текущего пользователя, сортировка по ID (новые сверху)
- Метод `store()` — валидация (название обязательное, комментарий необязательный), создание с привязкой к текущему пользователю, поддержка чекбокса "Отзыв"
- Добавлены 3 маршрута: `GET /sources` (sources.index), `GET /sources/create` (sources.create), `POST /sources` (sources.store)
- Создан шаблон `resources/views/sources/index.blade.php`:
  - Хлебные крошки "Главная > Источники" + баланс справа
  - Заголовок "Список источников" + зелёная кнопка "Создать" справа (компонент `x-page-header`)
  - Таблица: 4 столбца (ID, Название, Комментарий, Отзыв) — без фильтров (как на скриншоте 16.png)
  - Отзыв: зелёный текст "Да" или пусто
  - Чередование строк (тёмнее/светлее), сообщение "Нет источников" при пустом списке
  - Уведомление об успешном создании (зелёный flash-блок)
- Создан шаблон `resources/views/sources/create.blade.php`:
  - Хлебные крошки "Главная > Источники > Создать источник"
  - Заголовок "Создать источник"
  - Форма в тёмном блоке: поля Название (обязательное), Комментарий (textarea, необязательное), чекбокс "Отзыв", кнопки "Создать" и "Отмена"
  - Отображение ошибок валидации под каждым полем
- Сверка со скриншотом 16.png: структура соответствует эталону (заголовок + кнопка, таблица с 4 столбцами)
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-balance-badge`

### Этап 20: Страница "Актуальные города партнёров" (`/cities`)
- Создан `CityController` с методом `index(Request $request)` — получение всех городов системы с серверной фильтрацией
- Серверная фильтрация: по названию города (LIKE-поиск), по ID (LIKE-поиск), по доступности (выпадающий список Да/Нет/Все)
- Фильтр "Доступно партнёру?" по умолчанию установлен в "Да" — при первом открытии показываются только доступные города
- Сортировка по названию города (алфавитный порядок)
- Создан шаблон `resources/views/cities/index.blade.php`:
  - Хлебные крошки "Главная > Города" + баланс справа
  - Заголовок "Актуальные города партнёров"
  - Таблица: 4 столбца (Город, ID, Доступно партнёру?, Загруженность %)
  - Строка фильтров под заголовками: текстовый поиск по городу (применяется по Enter), текстовый поиск по ID (применяется по Enter), выпадающий список доступности (применяется при изменении, по умолчанию "Да")
  - Чередование строк (тёмнее/светлее), тёмная тема
  - Загруженность выровнена вправо, формат с одним десятичным знаком (например "8.7")
- Добавлен маршрут `GET /cities` → `CityController@index` (name: `cities.index`)
- Сверка со скриншотом 17.png: структура соответствует эталону (хлебные крошки, заголовок, таблица с фильтрами, доступность по умолчанию "Да")
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-balance-badge`

### Этап 21: Страница "Виды работ" (`/work-types`)
- Создан `WorkTypeController` с методом `index()` — получение всех видов работ из БД, сортировка по названию
- Создан шаблон `resources/views/work-types/index.blade.php`:
  - Хлебные крошки "Главная > Виды работ" + баланс справа
  - Заголовок "Виды работ" (без кнопки создания — справочная страница, только чтение)
  - Таблица: 3 столбца (Вид работ 20%, Описание 65%, Профильность 15%)
  - Столбец "Описание" — самый широкий, поддерживает многострочный текст
  - Профильность — простой текст "Профильная" / "Непрофильная" (без бейджей)
  - Нет строки фильтров (как на скриншоте 18.png)
  - Чередование строк (тёмнее/светлее), сообщение "Нет видов работ" при пустом списке
- Добавлен маршрут `GET /work-types` → `WorkTypeController@index` (name: `work-types.index`)
- Создан `WorkTypeSeeder` с 10 видами работ из скриншота-эталона (updateOrCreate для безопасного повторного запуска)
- Подключён `WorkTypeSeeder` к `DatabaseSeeder`
- Сверка со скриншотом 18.png: структура соответствует эталону (хлебные крошки, заголовок без кнопки, таблица с 3 столбцами, многострочные описания)
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-balance-badge`

### Этап 22: Страница "Работа с отзывами" (`/reviews`)
- Создан `ReviewController` с 3 методами: `index` (список с фильтрацией по датам), `create` (форма добавления), `store` (валидация + создание)
- Метод `index()` — получение отзывов текущего пользователя с eager load (order, city), фильтрация по дате создания (от/по)
- Метод `store()` — валидация (ID заказа обязателен + exists, URL обязателен + формат, город необязателен), проверка принадлежности заказа текущему пользователю, создание со статусом `new`
- Константа `STATUS_LABELS` — маппинг статусов отзывов (new → Новая, resolved → Решена, not_resolved → Не решена)
- Добавлены 3 маршрута: `GET /reviews` (reviews.index), `GET /reviews/create` (reviews.create), `POST /reviews` (reviews.store)
- Создан шаблон `resources/views/reviews/index.blade.php`:
  - Хлебные крошки "Главная > Негативные отзывы" + баланс справа
  - Заголовок "Работа с отзывами" + зелёная кнопка "Добавить ссылку на отзыв" справа (компонент `x-page-header`)
  - Фильтр по датам (компонент `x-date-filter`) — поля "Дата от" и "Дата по" с зелёной кнопкой применения
  - Таблица: 7 столбцов (ID, Заказ, Статус, Отзыв, Дата создания, Дата закрытия, Город)
  - Строка фильтров под заголовками: текстовый поиск по ID и Заказу (клиентская фильтрация), выпадающие списки по Статусу и Городу
  - Столбец "Статус" — цветные бейджи (синий = Новая, зелёный = Решена, красный = Не решена)
  - Столбец "Отзыв" — кликабельная ссылка с тултипом при наведении (превью URL)
  - Чередование строк (тёмнее/светлее), hover-эффект
  - Уведомление об успешном добавлении (зелёный flash-блок)
- Создан шаблон `resources/views/reviews/create.blade.php`:
  - Хлебные крошки "Главная > Работа с отзывами > Добавить ссылку на отзыв"
  - Заголовок "Добавить ссылку на отзыв"
  - Форма в тёмном блоке: выпадающий список ID заказа (обязательное), поле URL ссылки (обязательное), выпадающий список городов (необязательное), кнопки "Добавить" и "Отмена"
  - Отображение ошибок валидации под каждым полем
- Навбар уже содержал ссылки на `/reviews` и `/reports/reviews` — теперь маршрут `/reviews` зарегистрирован и работает
- Сверка со скриншотом 19.png: структура соответствует эталону (фильтр дат, кнопка добавления, таблица с 7 столбцами, тултип на ссылке)
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-date-filter`, `x-balance-badge`

### Этап 23: Страница "Отчёт по отзывам" (`/reports/reviews`)
- Добавлен маршрут `GET /reports/reviews` → `ReportController@reviews` (name: `reports.reviews`)
- Добавлен import модели `Review` в `ReportController`
- Создан метод `reviews(Request $request)` в `ReportController`:
  - Группировка отзывов текущего пользователя по `review_type`
  - Агрегация: COUNT total, COUNT resolved, COUNT (new + not_resolved)
  - Фильтрация по датам (date_from / date_to по полю created_at)
- Создан шаблон `resources/views/reports/reviews.blade.php`:
  - Хлебные крошки "Главная > Отчёт по отзывам"
  - Заголовок "Отчёт по отзывам" (компонент `x-page-header`)
  - Фильтр по датам (компонент `x-date-filter`) — поля "Дата от" и "Дата по"
  - Таблица: 4 столбца (Тип отзыва, Кол-во отзывов, Решено, Не решено)
  - Чередование строк (тёмнее/светлее)
  - Пустое состояние "Записей не найдено."
- Сверка со скриншотом 20.png: структура соответствует эталону (фильтр дат, таблица с 4 столбцами, текст при отсутствии данных)
- Используются компоненты: `x-breadcrumbs`, `x-page-header`, `x-date-filter`, `x-balance-badge`

### Этап 24: Сидеры (тестовые данные)
- Создан `CitySeeder` — 30 городов из скриншота 17.png (Абакан, Адлер, Анапа, ..., Омск), все с `is_available=true` и случайной загруженностью 3–30%
- Обновлён `WorkTypeSeeder` — 10 видов работ уже существовал с предыдущего этапа, данные актуальны
- Создан `UserSeeder` — 2 тестовых пользователя: test@superpart.ru (баланс 78 250 Р) и admin@superpart.ru (баланс 50 000 Р), пароль `password`
- Создан `SourceSeeder` — 5 источников из скриншота 16.png (Иван only НСК1, Сергей Тверь, Морозов Евгений, Логинов Сергей, Лосо Авито), привязаны к первому пользователю
- Создан `EmployeeSeeder` — 4 сотрудника из скриншота 15.png (Животносов Иван, Логинов Сергей Романович, Морозов Евгений Андреевич, Алиев Иса Лукманович), все активные, привязаны к первому пользователю
- Создан `OrderSeeder` — 25 заявок с реалистичными данными: все 8 статусов представлены (3–4 заявки каждого), разные города, виды работ, типы (first_time/warranty/repeat), 15 клиентов с реалистичными именами и телефонами
- Создан `TransactionSeeder` — 10 транзакций типа `charge` привязанных к заявкам, суммы 150–2500 Р, balance_after пересчитывается последовательно
- Создан `WithdrawalRequestSeeder` — 2 заявки на вывод: одна со статусом `completed`, другая `in_work`, каждая с 2 позициями (withdrawal_request_items), привязаны к реальным транзакциям
- Создан `ReviewSeeder` — 3 отзыва: Авито (новая), Telegram (решена), Яндекс Карты (не решена), привязаны к реальным заявкам и городам
- Обновлён `DatabaseSeeder` — порядок вызова сидеров с учётом FK-зависимостей: City → WorkType → User → Source → Employee → Order → Transaction → WithdrawalRequest → Review
- Все сидеры используют `updateOrCreate` где возможно для безопасного повторного запуска
- Проверка `php artisan migrate:fresh --seed` — 13 миграций и 9 сидеров выполнены без ошибок
- Итоги: 30 городов, 10 видов работ, 2 пользователя, 5 источников, 4 сотрудника, 25 заявок, 10 транзакций, 2 заявки на вывод (4 позиции), 3 отзыва

### Этап 25: Новый дизайн‑токен и светлая/тёмная тема
- В `resources/css/app.css` добавлен полноценный дизайн‑токен на CSS‑переменных для светлой (`:root`) и тёмной (`.dark`) тем
- Через `@theme inline` настроено соответствие переменных Tailwind‑цветам (`bg-background`, `bg-card`, `bg-secondary`, `text-foreground`, `border-border`, `bg-primary` и т.д.)
- Обновлены layout‑файлы (`layouts/app.blade.php`, `layouts/auth.blade.php`): класс `dark` на `<html>`, фон/текст через `bg-background text-foreground`, подключены шрифты Inter и JetBrains Mono через Bunny Fonts
- В навбаре (`partials/navbar.blade.php`) внедрён переключатель темы (светлая/тёмная) с сохранением выбора в `localStorage`, все цвета переведены на семантические классы
- Обновлены Blade‑компоненты (`balance-badge`, `breadcrumbs`, `data-table`, `dropdown-menu`, `page-header`, `tabs`) — замена хардкода hex‑цветов на токен‑классы; компонент `status-badge` сохранён без изменений (цвета как в макете)
- Все страницы (заявки, финансы, отчёты, справочники, отзывы, дашборд, логин) переведены с `bg-[#1a1a2e]`, `bg-[#1e1e30]`, `bg-[#2d2d3f]`, `text-white`, `border-gray-600` и т.п. на `bg-background`, `bg-card`, `bg-secondary`, `text-foreground`, `text-muted-foreground`, `border-border`, `bg-primary` и т.д.
- В `resources/js/app.js` добавлена функция `initTheme()` для инициализации темы и переключателя, а также обновлены цвета иконок сортировки в таблицах (используются `text-primary` / `text-muted-foreground` вместо `text-green-400` / `text-gray-600`)
